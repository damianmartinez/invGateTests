const config = {}

config.baseUrl =  'https://damianmard.cloud.invgate.net'
config.url = config.baseUrl
config.timeMs = 50000
config.nameCategory = 'Automation'

config.allowScreenshots = true

config.credentials = {
    username:  'superadmin',
    password:  '19bc91610'
}

module.exports = config
